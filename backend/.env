# Use the credentials you created in step 1
DATABASE_URL="postgresql+asyncpg://postgres:2002A.f1@localhost:5432/bragboardbd"

# Generate a strong secret key. You can use: openssl rand -hex 32
SECRET_KEY="replace_this_with_a_very_strong_random_key"
