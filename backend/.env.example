# Example env file - safe to commit
# Replace values before deploying to production
DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5432/projectx
SESSION_SECRET=please-change-me
DEBUG=false
FRONTEND_URL=http://localhost:5000

# Base URL for backend used in email verification link
APP_BASE_URL=http://localhost:8000

# SMTP configuration for sending emails
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USERNAME=email@example.com
SMTP_PASSWORD=PASSWORD
EMAIL_FROM=no-reply@example.com
COMPANY_APPROVER_EMAIL=admin@example.com